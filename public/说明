sass文件夹说明：

	bases：包含了最基础的样式重置和公共变量的定义（例如颜色、边框、字体大小、圆角度数等等）
	
	modules：包含了一些公用的模块，例如弹窗、按钮、表单、页头页脚等等

	pages：包含了各个页面对应的样式文件，例如 index.html 就对应 _index.scss

	mixins：包含了一些最常用的sass模块，例如三角形、透明度、点点点、清浮动等等


	约定：为避免样式命名冲突，每一条样式前可以加自己名字缩写，例如   yk-side、yk-banner
	           嵌套最多3层
